name: use my action
on:
  pull_request:

jobs:

  # TODO: move to composite action in action.yml, one step should call action-docker.yml
  test_action:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v4
    - name: holunder
      uses: ./
      with:
        service_account_key: "${{ secrets.SA_JSON_KEY }}"
        gdrive_root_folder_id: 1TUSaFwWGfw0GZyWzYxJzjTpTYU-KD9z8
        local_markdown_root_folder: synced
        gdrive_management_spreadsheet: 1f_d4E6w8dgVy0xDcm-pTUZma6CGUJhr1bLquj8H3RoM
        ignore_images: True
        commit_message: Add updated markdown files
        file_pattern: synced/
